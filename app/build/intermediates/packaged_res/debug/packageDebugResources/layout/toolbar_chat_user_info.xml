<?xml version="1.0" encoding="utf-8"?>
<!--
Layout para mostrar la información de un usuario (por ejemplo en la toolbar del chat).
Incluye: ícono de usuario + nombre + estado.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingStart="8dp"
    android:paddingEnd="8dp">

    <!-- Ícono de usuario (avatar por defecto o imagen del perfil) -->

    <ImageView
        android:id="@+id/ivUserIcon"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:src="@drawable/ic_user_circle"
        android:contentDescription="Icono del usuario"
        android:layout_marginEnd="12dp" />

    <!-- Contenedor vertical para nombre y estado -->

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Nombre del usuario -->

        <TextView
            android:id="@+id/tvUserName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Nombre del Usuario"
            android:textColor="@color/black"
            android:textSize="16sp"
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end" />

        <!-- Estado del usuario (ej: Online, Escribiendo...) -->

        <TextView
            android:id="@+id/tvUserStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Online"
            android:textColor="@color/turquoise_blue"
            android:textSize="12sp"
            android:maxLines="1"
            android:ellipsize="end" />

    </LinearLayout>

</LinearLayout>