<?xml version="1.0" encoding="utf-8"?>
<!--
Item de chat para MENSAJES ENVIADOS (a otro usuario).
Incluye un contenedor vertical con:
- Burbuja de texto (mensaje).
- Imagen opcional (si el mensaje incluye una foto).
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingVertical="4dp">
    <!--
    Burbuja de texto del usuario actual:
    - wrap_content para ajustarse al texto.
    - gravity=end para alinear a la derecha.
    - background usa un drawable con estilo de burbuja (bg_bubble_mine).
    - padding interno para que el texto no quede pegado al borde.
    -->
    <TextView
        android:id="@+id/tvMsgMine"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end"
        android:background="@drawable/bg_bubble_mine"
        android:padding="10dp"
        android:textColor="@android:color/white"
        android:textSize="16sp" />
        
    <!--
    Imagen enviada por el usuario actual:
    - Tamaño fijo de ancho (220dp) para mantener consistencia.
    - adjustViewBounds="true" ajusta el alto proporcionalmente según la imagen.
    - gravity=end para alinearla a la derecha (como el texto).
    - visibility="gone" por defecto: solo se muestra si el mensaje tiene imagen.
    - marginTop separa la imagen del texto si ambos existen.
    -->
    <ImageView
        android:id="@+id/ivImageMine"
        android:layout_width="220dp"
        android:layout_height="wrap_content"
        android:adjustViewBounds="true"
        android:layout_gravity="end"
        android:contentDescription="@string/app_name"
        android:visibility="gone"
        android:layout_marginTop="4dp" />

</LinearLayout>
